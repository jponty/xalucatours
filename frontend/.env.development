# Local development uses the deployed Supabase-backed API.
# A developer can override this in .env.development.local when running
# the backend locally.
REACT_APP_BACKEND_URL=https://xaluca-tours-api.onrender.com
