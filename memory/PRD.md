# Xaluca Tours — PRD / Estado

## Problema original
App full-stack (React + FastAPI + MongoDB) para agencia de viajes a medida por Marruecos, heredando el sistema de diseño premium "Grup Xaluca" (Dark Academia, zellige, acentos dorados #D4A373). Trilingüe (es/en/fr), mapas Leaflet, CMS global editable por slots, audio global persistente, notificaciones por email (Resend), galería Pexels/Unsplash, precios por ruta.

## Arquitectura
- backend/server.py — CMS slots, leads CRUD, files, integraciones, Resend
- frontend/src/lib/{routes.js, routeComponents.js, allTripsCatalog.js, pricing.js, programPricing.js, preciosData.js, homeCarousels.js}
- frontend/src/pages/{PreciosPage.jsx, AdminPage.jsx}
- Idioma por defecto (es) en raíz; en/fr bajo /<lang>/<slug>

## Implementado (jun 2026)
- **Widget de contacto tipo bola en "Estilos de viaje" (jun 2026) — COMPLETADO + VERIFICADO (screenshot)**:
  - Nuevo `components/TravelHelpWidget.jsx` insertado en `TravelCategories` justo ENCIMA de las cards de navegación. Píldora crema ligera con avatar circular (bola) terracota + monograma X blanco + anillo con pulso (`animate-ping`) + punto "online" verde. Texto editable por CMS ("¿Necesitas ayuda?" / "Habla con un agente y encuentra tu viaje ideal.") y botón "Solicitar cita" → `pathFor(lang,"appointment")` (= `/citaprevia`). testids `travel-help-widget` / `travel-help-widget-cta`. Slots `home.cat.help.{title|body|cta}`. Verificado en home (href `/citaprevia`).

- **CTAs en "Por qué Xaluca Tours" (jun 2026) — COMPLETADO + VERIFICADO (screenshot)**:
  - Añadido un CTA con verbo de acción al final de cada uno de los 4 bloques (`components/WhyXaluca.jsx`), con flecha animada (hover gap), texto trilingüe editable por CMS (slots `home.why.{01..04}.cta`) y enlace localizado vía `pathFor`: 01→Descubrir viajes `/viajes` (toursLanding); 02→Planificar mi viaje `/planifica-tu-viaje` (planTrip); 03→Solicitar cita previa `/citaprevia` (appointment); 04→Hablar con un especialista `/contacto` (contact). testid `why-card-cta-{num}`. Verificado en home (hrefs correctos).

- **Card CTA "Solicitar cita previa" en Bloques culturales (PILOTO en tourAtlasDesierto67) (jun 2026) — COMPLETADO + VERIFICADO (screenshot)**:
  - En "Bloques culturales destacados" (`ProgramTemplate.DayBlock`, grid de 2 columnas), cuando el nº de bloques culturales es IMPAR se añade automáticamente una card CTA que rellena la celda vacía y equilibra la composición (p.ej. 3 → 4).
  - Nuevo `components/DayCultureCTA.jsx`: avatar circular con el monograma X blanco sobre el color de acento del día, eyebrow "Xaluca Tours · Especialistas", titular "¿Tienes alguna duda sobre este viaje?", mensaje invitando a hablar con un especialista y botón "Solicitar cita previa" → `pathFor(lang,"appointment")` (= `/citaprevia` en ES, localizado en EN/FR). Fondo espresso `#2C2621` para destacar como CTA. Textos editables por CMS (slots globales `program.culture_cta.{title|body|button}`). testid `day-culture-cta` / `day-culture-cta-button`.
- **Card CTA "Solicitar cita previa" en Bloques culturales — EXTENDIDA A TODOS LOS PROGRAMAS (jun 2026) — COMPLETADO + VERIFICADO (screenshot)**:
  - Se eliminó el gating por `routeId`; ahora la card `DayCultureCTA` se añade en CUALQUIER programa cuando el nº de bloques culturales del día es impar (rellena la celda vacía del grid 2×2). Verificado también en `marrakech_ergchebbi/programa_6n_7d` (7 días → 7 cards, botón → `/citaprevia`, avatar con acento del día).



- **Testimonio por día en TODOS los programas (jun 2026) — COMPLETADO + VERIFICADO (screenshot)**:
  - El bloque `DayTestimonial` ahora se muestra en cada día de cada programa (`ProgramTemplate.DayBlock`), en la columna lateral, justo debajo de "Asistente Virtual"/"Contactar". Un testimonio DISTINTO por día.
  - `lib/dayTestimonials.js` `getDayTestimonial(routeId, day, dayIndex)`: (1) overrides curados (solo `tourAtlasDesierto67`, orígenes corregidos a ciudades españolas); (2) resto AUTO-DERIVADO del contenido del día: detecta lugar icónico (Erg Chebbi, Aït Ben Haddou, Chefchaouen, Todra, Dades, Essaouira, Volubilis/Meknes, Agafay, M'Goun, Rissani, Khamlia, Ziz, Ouarzazate, Rabat, Fez, Marrakech…) o tema (llegada, desierto, montaña, gargantas, ciudad, costa, mercado, oasis, relax, regreso) → cita trilingüe acorde.
  - Orígenes SIEMPRE de España (lista de 30 ciudades) y variados: autor + ciudad espaciados por posición del día con pasos coprimos (11 sobre 26 autores, 7 sobre 30 ciudades) → cada día del programa tiene viajero+ciudad DISTINTOS, deterministas y estables entre recargas. Cita editable por CMS (slots `trip.{routeId}.day.{idx}.testimonial.*`).
  - Verificado en `/viajes/marrakech_ergchebbi/programa_6n_7d` (7 días, 7 ciudades únicas españolas, citas acordes a cada día) y en el piloto `atlas_desierto/programa_6n_7d` (curados).


- **Testimonio por día en páginas de viaje (PILOTO en tourAtlasDesierto67) (jun 2026) — COMPLETADO + VERIFICADO (screenshot)**:
  - Nuevo bloque `components/DayTestimonial.jsx` en la columna lateral de cada día, JUSTO debajo de los botones "Asistente Virtual" / "Contactar" (`ProgramTemplate.DayBlock`). Muestra un testimonio ESPECÍFICO del día que se está viendo (cita + autor + origen + 5 estrellas + badge "Viajero verificado").
  - Datos en `lib/dayTestimonials.js` (`getDayTestimonial(routeId, dayId)`), trilingüe; PILOTO solo para `tourAtlasDesierto67` (7 días). Devuelve null en el resto → el bloque no se renderiza (gating).
  - Editable por CMS: `quote`/`author`/`origin` vía `<EditableText>` con slots únicos por viaje+día (`trip.${routeId}.day.${dayIndex}.testimonial.{quote|author|origin}`). Visual dark-academia (papel cálido `#F8F2E6`, borde de acento del día, estrellas `#D4A373`, marca de agua de comillas). testid `day-testimonial-<dayId>`.
  - Verificado en `/viajes/atlas_desierto/programa_6n_7d`: Día 1 → "Marta & Javier · Madrid" sobre Ouarzazate/Hotel Xaluca Dades. PENDIENTE decisión usuario: extender al resto de programas tras evaluar el piloto.


- **Primera sección desplegable abierta por defecto en páginas de viaje (jun 2026) — COMPLETADO + VERIFICADO (screenshot)**:
  - Petición: en cada página de viaje, que la PRIMERA etapa/lugar aparezca desplegada por defecto al cargar; tras interacción, comportamiento normal de acordeón; al refrescar / nueva sesión, vuelve a abrirse la primera (sin persistencia).
  - Solución (solo inicializadores de `useState`, sin persistencia → resetea en cada carga): `TripRouteMap` ("El recorrido completo") `activeNode` → primera etapa con detalle (índice 0). `DayRouteMap` ("Mapa del día"): `LandmarkMode.activeId` → `landmarks[0].id`; `WaypointMode.activeIdx` → primer waypoint con perfil; `StayInteractive.open` → `true`. Al abrir el primer lugar se muestra también su "Galería del lugar" (`LandmarkCarousel`).
  - Verificado en `/viajes/atlas_desierto/programa_6n_7d`: recorrido completo con etapa 01 abierta (resto colapsado); cada uno de los 7 "Mapa del día" con su primer lugar abierto y su galería visible. Toggle/acordeón intactos.


- **Informe de cobertura de migración de imágenes (jun 2026) — COMPLETADO + VERIFICADO (curl + screenshot)**:
  - Objetivo: saber con métricas exactas cuándo es 100% seguro eliminar el sistema de fallback del código.
  - Backend (`server.py`): nuevo `GET /api/admin/migrate-fallbacks/coverage` (auth) que calcula EN VIVO desde la BD (independiente del job en curso): SLOTS — total registrados (`image_slot_registry`), cuántos con fallback EXTERNO, y de esos cuántos ya en CMS (`/api/files`), vaciados (`cleared`) y PENDIENTES (+ muestra de 30 ids); también `local_fallback`/`no_fallback`. URLs directas `<Img>` — total registradas (`remote_image_registry`), migradas (en `db.files.migrated_from` = url-map) y pendientes (+ muestra de 30). `overall` con `percent`, `done/total` y flag `safe_to_remove`.
  - Frontend (`AdminPage.jsx`, pestaña Mirror DB): nuevo bloque `CoverageReport` dentro de `MigrateFallbacksPanel` — medidor global (verde si 100%/seguro, dorado si pendiente), 2 barras (slots / URLs directas), desglose numérico, muestras de pendientes y botón "Actualizar" (`coverage-refresh-btn`). Se refresca al montar y al terminar una migración. Testids: `coverage-report`, `coverage-overall-percent`, `coverage-safe-flag`, `coverage-slots-bar`, `coverage-urls-bar`, `coverage-pending`.
  - Verificado en preview: endpoint devuelve `slots 1032/1032 · urls 5/5 · overall 100% · safe_to_remove=true`; el panel renderiza "100% centralizado — es seguro eliminar el sistema de fallback del código".


- **Fase 2 — Centralizar también las imágenes `<Img>` directas (no-slot) vía mapa URL→CMS (jun 2026) — IMPLEMENTADO y VERIFICADO en preview**:
  - Problema: el contenido `<Img>` directo (miniaturas de tarjetas, postales, pósters de vídeo, related/peer-nav) **hotlinkea Unsplash/Pexels** (resize por params nativos en `imageUrl.js`), no se sirve desde el storage. Convertirlo a slots editables duplicaría imágenes que son espejo de heros ya slotted.
  - Solución (mapa de resolución, no slots): `Img.jsx` ahora (1) **registra** cada URL remota Unsplash/Pexels que renderiza (`POST /api/image_urls/register`, batched/deduped) y (2) carga una vez `GET /api/image-url-map` y **resuelve** `src` remoto → su copia `/api/files`. Backend: colección `remote_image_registry` + ambos endpoints; el job `migrate-fallbacks` AMPLIADO para importar también esas URLs registradas (reusa `_import_remote_image`, dedupe). El mapa = `db.files.migrated_from → /api/files`.
  - Verificado e2e: registro→migración→mapa resuelve a `/api/files`; migración 0 errores. **Home: 261 imgs → 111 desde `/api/files`, 0 Unsplash, 0 Pexels** (resto = logos/assets locales + tiles de mapa). Página intacta. Panel admin "Centralizar imágenes en el CMS" actualizado (cubre slots + directas).
  - Para PRODUCCIÓN: igual que Fase 1 — redeploy → navegar/tráfico (puebla ambos registros: slots + URLs directas) → pulsar "Centralizar fallbacks en el CMS". El render de fallback sigue como red de seguridad.
  - Nota menor: variantes de ancho distintas de una misma foto se importan por separado (bounded; masters ≤2000px WebP). Optimizable normalizando params en el futuro.

- **Centralización de imágenes en el CMS: registro de slots + migración de fallbacks (jun 2026) — IMPLEMENTADO y VERIFICADO en preview; pendiente repetir en producción tras deploy**:
  - Objetivo: que TODAS las imágenes (las ya editadas + los fallbacks de código que aún viven en `lib/`) estén centralizadas y gestionables desde el CMS, manteniendo el render de fallback como red de seguridad (decisión usuario A-a + B-a: solo capa `EditableImage`).
  - Backend (`server.py`): (1) **Registro de slots de imagen** — colección `image_slot_registry` + `POST /api/image_slots/register` + `GET /api/image_slots/registry` (espejo del registro de textos). (2) **Importador genérico** `_import_remote_image(url)`: descarga → `optimize_image` (WebP) → `put_object` → registro en `db.files` (`source:"fallback"`, `tags:["library","fallback-migrated"]`, `migrated_from`), con **dedupe por `migrated_from` y por sha256** (no duplica). (3) **Job de migración** `_run_migrate_fallbacks` + `POST /api/admin/migrate-fallbacks` + `/status` (background, idempotente, throttled, OOM-safe): para cada slot del registro con fallback externo NO guardado en CMS → importa y fija `image_slots[slot] = /api/files/...`. Respeta lo ya editado o `cleared`.
  - Frontend: `EditableImage` registra `{slot, fallback, alt}` al renderizar (batched, fire-and-forget) vía `registerImageSlot`. Nuevo panel admin **"Centralizar imágenes en el CMS"** (`MigrateFallbacksPanel`, pestaña Mirror DB) con barra de progreso. El render de fallback se MANTIENE como red de seguridad (no se quitó).
  - Verificado e2e en preview: crawl de 114 rutas ES → 657 slots registrados → migración 100% (**134 importadas, 303 reutilizadas/dedupe, 219 ya guardadas, 0 errores** tras limpiar el slot de prueba). `image_slots` con imagen: 1025 → **1462**. Slots antes vacíos (ej. `home.intro.*`) ahora sirven desde `/api/files` (http 200). Backend sano durante todo el proceso.
  - Para PRODUCCIÓN: redeploy → navegar por las páginas (o dejar tráfico) para poblar el registro → pulsar "Centralizar fallbacks en el CMS". Cuando se confirme 100% de cobertura, se podrá quitar el sistema de fallback del código (paso diferido, opcional).
  - NOTA: el contenido `<Img>` directo (testimonios, juego, POIs, algunas tarjetas) NO tiene slot CMS → fuera de alcance (2ª fase: convertir a slots editables).

- **CMS como única fuente de verdad de imágenes: eliminado el render de fallbacks de código (jun 2026) — IMPLEMENTADO y luego REVERTIDO a petición del usuario**:
  - NOTA DE ESTADO: se implementó (CMS-only en `EditableImage`) y se verificó, pero al ver el impacto (home 54 / página de viaje 27 slots pasaban a "Sin imagen" porque hay muchos más `EditableImage` que los ~1.041 slots guardados) el usuario pidió volver al estado anterior. **REVERTIDO** (los 4 cambios de render + el testid `cms-empty-image`). Los fallbacks de código vuelven a mostrarse. Nada se perdió: los fallbacks son URLs en `lib/`, y las imágenes reales del CMS viven en Mongo (intactas).
  - Si se retoma: el enfoque seguro sería poblar primero los slots vacíos (Edit Mode / informe de "imágenes faltantes" en admin / import de defaults al storage) ANTES de quitar fallbacks, para no dejar huecos.

- **Auto-mantenimiento de imágenes en arranque: re-optimización + warm-up serializados (jun 2026) — APLICADO en preview, VERIFICADO; requiere REDEPLOY**:
  - Objetivo (continuación del fix 520): que los masters gigantes se encojan SOLOS tras cada arranque (sin pulsar el botón manual) y luego se calienten, sin reintroducir el riesgo OOM.
  - `server.py`: nuevo flag `IMG_REOPT_ON_STARTUP` (default ON). El startup ahora lanza `_startup_maintenance()` (delay 25s, todo en try/except) que ejecuta SECUENCIALMENTE: (1) `_run_reoptimize_library()` (encoge masters >500KB a WebP ≤2000px, uno a uno, throttle, idempotente vía `reoptimized_at`), y SOLO al terminar (2) `_run_warm_cache()` (con la guardia `WARM_MAX_MASTER_BYTES`). Serializar evita que los dos trabajos pesados en memoria se solapen → nunca doblan el pico de RAM.
  - `optimize_image`: añadido `img.draft(None, (MAX_IMAGE_WIDTH, MAX_IMAGE_WIDTH))` tras `Image.open` → decodifica a escala reducida los JPEG gigantes (recorta drásticamente memoria y tiempo del decode; no-op en PNG). Misma técnica que `_encode_variant`.
  - Verificado: reopt manual real corriendo limpio (125/2106, **0 errores, ~414 MB liberados** y subiendo) con backend SANO durante el proceso (`/api/pricing` 200, listado `/api/files` 200); subida de imagen única → WebP OK; `tests/test_image_optimize.py` 2/2 y `test_files_listing.py` 14/14. Sin regresiones (los fallos de `test_contact_api` = endpoint con auth 401 y `test_image_library` = dedup sha256 en re-ejecución son PRE-EXISTENTES, no relacionados con estos cambios — confirmado con subida única exitosa).
  - Tras REDEPLOY: en cada arranque, producción auto-encoge sus masters gigantes (p.ej. el PNG de 9,7 MB) y luego calienta variantes; el botón manual "Re-optimizar Biblioteca" sigue disponible. Para desactivar: `IMG_REOPT_ON_STARTUP=false`.

- **Cloudflare 520 en `/api/*` — guardia de memoria en el warm-up (jun 2026) — APLICADO en preview, VERIFICADO; requiere REDEPLOY**:
  - Reproducción: el LISTADO `GET /api/files` devuelve 200 con JSON solo-metadatos paginado, rápido, TANTO en preview (0,25-0,3s) COMO en producción (0,68s) → el código del listado YA es robusto (proyección solo-metadatos, `max_time_ms(8000)`, `count_documents(maxTimeMS=4000)`, todo en try/except → SIEMPRE 200 con JSON válido). No es la causa de un 520 sostenido.
  - RCA del 520: el warm-up de caché en arranque (default ON) DECODIFICA cada master de `db.files`. Producción tiene masters gigantes sin optimizar (p.ej. PNG de **9,7 MB** → ~cientos de MB al rasterizar) → pico de RAM → OOM → el backend cae/bucle de reinicio → Cloudflare 520 en TODOS los `/api/*`, incluido el listado. Coincide con el incidente OOM previo.
  - Fix (`server.py`): nueva guardia `WARM_MAX_MASTER_BYTES` (env `IMG_WARM_MAX_MASTER_BYTES`, default **4 MB**). El warm-up batch (`_run_warm_cache`, ahora lee `size` por archivo) y el warm de imagen única (`_warm_one_path`) SALTAN decodificar cualquier master > umbral → el warm-up NUNCA puede OOM-matar el contenedor. Los gigantes se sirven bajo demanda (raro) y deben encogerse con "Re-optimizar Biblioteca".
  - Verificado: warm-up corre limpio (total=4228, done=4228, errors=0), backend sano durante el proceso (`/api/pricing` 200), listado 200 en 0,25s, suite `tests/test_files_listing.py` 14/14 OK.
  - ACCIÓN USUARIO: **redesplegar** para aplicar; luego (opcional, recomendado) pulsar "Re-optimizar Biblioteca" en el admin de PRODUCCIÓN para encoger los masters gigantes (así se optimizan + calientan en vez de saltarse).

- **Modo Edición de imágenes UNIFICADO en páginas de viaje (galería por día) — COMPLETADO + VERIFICADO (testing_agent iteration_51, frontend 100%)**:
  - Las galerías por día de las páginas públicas (`DayImageGallery.jsx`) ahora son editables INLINE en Modo Edición de imagen, con PARIDAD TOTAL con el Admin: reemplazar, añadir, eliminar, reordenar (drag&drop), fijar principal, subir desde PC y elegir de la Biblioteca.
  - Decisión usuario (b): en Modo Edición se OCULTA el overlay de edición por imagen y se unifica TODO en un único botón "Editar galería" (`day-gallery-edit-<dayId>`) sobre el visor → abre un modal portal (`day-gallery-editor-modal-<dayId>`, role=dialog, whitelisted con `data-edit-allow`) con el MISMO `<DayGalleryEditor>` que usa el Admin, sobre el MISMO registro `day_galleries/{galleryKey}` (clave index-based `<ns>.day.<index>.<dayId>`; lee primero la nueva, fallback a la legacy id-based → migra al editar). Fuente única de verdad: editar inline o desde Admin/Galerías queda sincronizado.
  - `DayGalleryEditor.jsx` extraído como componente reutilizable (Admin + inline). Nuevo helper compartido `lib/dayGalleryStore.js#buildDaySeed` (semilla idéntica en Admin e inline: principal CMS/`day.image` + `day.gallery[]` + overrides legacy de slides, deduplicado) → "lo que ves es lo que editas". `EditableImage.jsx` exporta `getSlotUrl`/`ensureSlotsLoaded` para sembrar desde los overrides de `image_slots`. En Modo Edición las slides legacy se renderizan como `<Img>` plano (sin overlay).
  - Persistencia backend ya verificada por curl (PUT→GET→DELETE round-trip OK en `/api/day-galleries/{key}`). e2e verificado: delete/add-desde-biblioteca/setMain/reorder/upload persisten al instante, el contador del visor (`day-gallery-counter-<dayId>`) y el Admin reflejan los cambios; recarga conserva los cambios.
  - Backlog menor (no bloqueante, de iteration_51): en `DayGalleryEditor.onUpload` el PUT de pre-guardado se ejecuta aunque la galería ya esté persistida (idempotente, request redundante); exponer un `data-testid` en el botón "Añadir" del `ImageLibraryPicker`.

- **Mejora: auto-optimización/warm-up al editar inline + aviso "Cambios publicados" (jun 2026) — COMPLETADO + VERIFICADO (self-test)**:
  - Backend (`server.py`): nuevos helpers `_path_from_files_url` + `_warm_gallery_images`. Ahora `PUT /api/day-galleries/{key}` y `PUT /api/slots/{slot_id}` lanzan `_warm_one_path` (fire-and-forget, idempotente) para CADA imagen hospedada en nuestro storage (`/api/files/...`) → cubre el hueco de las imágenes elegidas desde la Biblioteca (que no pasaban por el hook de subida), de modo que sus variantes AVIF/WebP se calientan al instante. Las subidas de archivo ya calentaban vía el hook on-upload. Verificado: PUT con url `/api/files/...` → 200, sin errores en backend.
  - Frontend (`DayGalleryEditor.jsx`): tras cada guardado, toast `sonner` "Cambios publicados · La galería se actualizó en la web al instante" (id fijo `day-gallery-saved` para colapsar guardados rápidos) + pastilla verde "✓ Publicado" en la cabecera del editor (testid `gallery-published-<galleryKey>`). El `Toaster` ya está montado en `Layout` (bottom-right). Verificado por screenshot (toast + pastilla visibles tras set-main).


- **Fix + endurecimiento de `GET /api/files` (listado) — VERIFICADO por testing_agent (iteration_50, 14/14, 100%); requiere REDEPLOY para producción**:
  - RCA: el listado era la lectura más pesada (sort por `created_at` SIN índice + `count_documents` sin límite sobre ~4k+ docs) → bajo carga superaba el timeout del gateway → Cloudflare 520 en producción (preview iba bien).
  - Fix en `server.py`: índices en startup (`db.files` `[(is_deleted,1),(created_at,-1)]` + `storage_path`); proyección SOLO metadatos (nunca bytes); `.max_time_ms(8000)` en el cursor y `count_documents(maxTimeMS=4000)` con fallback; todo en try/except → SIEMPRE devuelve JSON válido (200) aunque falle. `limit` cap 1..200, `skip` normalizado.
  - Verificado (testing_agent + curl): JSON válido, items solo metadatos (id, url, storage_path, original_filename, content_type, size, slot_id, tags, created_at — sin contenido binario), paginación OK, edge cases (skip/limit negativos, q largo, regex) → 200, ~160ms sobre 4228 docs. Regresión: `GET /api/files/{path}` sigue sirviendo bytes de imagen (AVIF/WebP). Suite: `/app/backend/tests/test_files_listing.py`.
  - Nota: el 520 real es de producción (infra/Cloudflare); el fix de código surte efecto tras REDEPLOY. Si tras redeploy persiste 520 en TODOS los `/api/*` → infra (Emergent Support).

- **Backfill "Re-optimizar Biblioteca" (jun 2026) — APLICADO en preview, VERIFICADO e2e; requiere REDEPLOY**:
  - Backend: job en segundo plano `_run_reoptimize_library()` + endpoints `POST/GET /api/admin/reoptimize-library[/status]` (auth). Busca masters en `db.files` >500KB sin `reoptimized_at`, los re-encoda con `optimize_image` (≤2000px WebP) y SOBRESCRIBE la misma `storage_path` (no rompe ninguna referencia de slot/galería). Tras cada uno: `_invalidate_path_cache` (borra variantes AVIF/WebP/orig cacheadas en disco para esa ruta) + `_warm_one_path` (re-calienta). Secuencial, throttle, try/except por archivo, idempotente (filtro `reoptimized_at`).
  - Frontend: `ReoptimizeLibraryPanel` en la pestaña Mirror DB (junto a Mirror + Warm-up), con barra de progreso y MB liberados (testids `reoptimize-library-*`).
  - Verificado e2e: 3346 masters candidatos; master de **5,7MB → 749KB**; backend RECEPTIVO durante el job (`/api/pricing` 0,002s); 0 errores; ~43MB liberados en el primer lote; variantes se regeneran bien (AVIF 960px 102KB en 0,3s); panel renderiza OK en el admin.
  - Tras REDEPLOY: pulsar "Re-optimizar Biblioteca" en el admin de PRODUCCIÓN (su DB es independiente) para encoger los masters gigantes existentes en producción.

- **Masters de stock optimizados al importar (jun 2026) — APLICADO en preview, VERIFICADO e2e; requiere REDEPLOY**:
  - Antes, Pexels import / Pexels bulk-fill / Unsplash import guardaban el ORIGINAL crudo (Pexels ~5–6 MB). Ahora los 3 pasan por `optimize_image` (≤2000px + WebP q80, helper ya usado por library/upload) ANTES de `put_object`, y disparan `_warm_one_path` para calentar el nuevo master al instante.
  - Verificado e2e: import real de Pexels → master `image/webp` de **~165 KB** (antes ~5,7 MB → **~97% menos**); variantes bajo demanda desde el nuevo master: cold AVIF w960 **0,16s**, WebP w1600 **0,53s**.
  - Beneficio: menos almacenamiento, primer encode y warm-up mucho más rápidos (decodifica 165KB/2000px en vez de 5,7MB/6000px). Aplica a imports NUEVOS; los masters gigantes ya existentes siguen igual hasta re-importar (posible backfill como tarea opcional).

- **Rendimiento de imágenes: codificación rápida + warm-up seguro reactivado (jun 2026) — APLICADO en preview, VERIFICADO; requiere REDEPLOY**:
  - Causa de la lentitud: tras desactivar el warm-up (para frenar el 520), la caché de imágenes de producción quedaba FRÍA → cada imagen se codificaba bajo demanda en la 1ª visita; medido en preview: WebP `method=6` sobre originales de ~5,7 MB tardaba **hasta ~16s** la primera vez. Además `IMG_CACHE_DIR=/app/backend/img_cache` es efímero (se borra en cada redeploy).
  - `server.py` — acelerada la codificación en `_encode_variant` y `_encode_all_variants`: WebP `method` 6→4 y `Image.draft()` (decode JPEG a escala reducida hacia el ancho objetivo). Resultado medido: cold WebP/AVIF ahora **~0,5-1,1s** (consistente), caliente ~0,17s.
  - Warm-up reactivado y endurecido: `IMG_WARM_ON_STARTUP` default ON; secuencial (`_WARM_INFLIGHT=_WARM_WORKERS`, default 1 → 1 imagen a la vez en hilos, sin procesos); throttle `IMG_WARM_THROTTLE` (0,05s) entre imágenes; delay de arranque 25s; todo en try/except. Verificado: warm-up corre en background (4227 variantes, 0 errores) y el backend responde al instante durante el proceso (`/api/pricing` 0,002s). Smoke test: página de programa con 116 imágenes, 0 rotas.
  - Tras REDEPLOY: la caché se auto-calienta en segundo plano tras cada arranque (se auto-cura el disco efímero) y las cargas bajo demanda son rápidas. Opcional: `IMG_WARM_WORKERS=2` para calentar más rápido; disco persistente para la caché = infra (Emergent Support).

- **Endurecimiento del warm-up de imágenes + fix de deploy (jun 2026) — APLICADO en preview, VERIFICADO; requiere REDEPLOY**:
  - Incidencia: producción (xalucatravel.com) devolvía HTTP 520 en TODOS los `/api/*` (backend caído/bucle de reinicio) tras redeploy; home estática OK; preview sano. Causa probable: el warm-up de caché (ProcessPoolExecutor con cpu-2 procesos codificando AVIF en arranque) tumbaba por OOM un contenedor con poca RAM.
  - `server.py`: warm-up de arranque DESACTIVADO por defecto (env `IMG_WARM_ON_STARTUP`, default off); por defecto codifica en threads (`IMG_WARM_WORKERS`=1, sin spawnear procesos; >1 usa ProcessPoolExecutor); todo envuelto en try/except → nunca tumba el backend. On-upload hook (thread, 1 imagen) y proxy `/api/files` on-demand intactos; botón admin "Optimizar imágenes" sigue disponible para warm-up manual.
  - `.gitignore`: eliminados patrones amplios `.env`/`.env.*`/`*.env` (bloqueaban backend/.env y frontend/.env en deploy) + negaciones explícitas. `deployment_agent`: PASS.
  - Verificado en preview: backend arranca limpio (200), log "warm-up on startup disabled", status running=False. **ACCIÓN PENDIENTE DEL USUARIO: redesplegar para recuperar producción.**

- **Cross-sell "También te puede interesar" en escapadas (jun 2026) — COMPLETADO + VERIFICADO (self-test)**:
  - Nuevo componente `components/RelatedJourneys.jsx` (banda oscura premium, acento dorado #D4A373): desde una escapada sugiere 2 itinerarios más largos/de mayor valor (card con héroe del hub destino, sección, título, subtítulo y CTA "Ver itinerario").
  - `programNav.js`: mapa `CROSS_SELL` (keyed por hubRouteId para escapadas con hub, o por routeId para directas) + helper `relatedJourneys(routeId)` que resuelve hubs registrados en HUB_NAV (links seguros) y excluye el propio hub. Targets: Desierto→{Atlas-Desierto Sur, Gran Sur Fez-Rak}; Atlas→{Atlas-Desierto, Marrakech Loop}; Rak-Erg-Rak→{Marrakech-Erg, Gran Sur Tánger-Rak}; Fez→{Ciudades Imperiales, Gran Sur Fez-Rak}; Marrakech→{Marrakech-Erg, Marrakech Loop}; Tánger→{Tánger-Fez, Ciudades Imperiales}.
  - Montado en `ProgramTemplate` (tras HubPeerNav) y en `EscapadaIntroPage` (tras ContactBand) → cubre programas de escapada, programas DIRECTOS (Desierto/Atlas, que no tenían peer-nav) e intros (Fez/Marrakech/Tánger). Devuelve null si no hay cross-sell curado (no afecta a otras páginas).
  - Verificado por screenshots: Desierto (sin peer-nav → cross-sell con 2 cards), Fez 2n3d (peer-nav + cross-sell), Tánger intro (cross-sell). Frontend compila.

- **Hubs de Escapadas cortas — cableado completo en `tourShort` (jun 2026) — COMPLETADO + VERIFICADO (self-test, 9 páginas)**:
  - Creados 3 hubs editoriales en `itineraryHubs.js` (solo para programNav; las páginas siguen usando `EscapadaIntroPage`): `HUB_ESCAPADA_FEZ` (`tourEscapadaFez` → fez23, fez34, fezSidiali34, fezSidiali45), `HUB_ESCAPADA_MARRAKECH` (`tourEscapadaMarrakech` → marrakech23, rakAgafay34), `HUB_ESCAPADA_TANGER` (`tourEscapadaTanger`, sin programas hijos: solo da breadcrumb a la intro). Añadidos a `HUBS_BY_ID`.
  - `programNav.js`: registrados en `SECTION_HUBS.tourShort` + nuevo `SECTION_DIRECT_PROGRAMS.tourShort` para escapadas de UN solo programa sin hub intermedio (`tourEscapadaDesierto34`, `tourEscapadaAtlas34`) → breadcrumb `Inicio › Viajes › Escapadas cortas › <label>` (sin nivel hub). Loop de build amplía `PROGRAM_NAV` con flag `direct` + `label` trilingüe.
  - `Breadcrumbs.jsx`: rama de programa soporta `direct` (omite crumb de hub, usa `label`). `HubPeerNav.jsx`: guard `if (!nav || !nav.hub) return null` para no renderizar peer-nav en programas directos.
  - Decisión: los 2 programas Fez-Sidiali se agrupan bajo el hub Fez (su parent natural; no existe ruta de hub propia) → quedan como peers de fez23/fez34. Blurbs distintos los diferencian aunque compartan "3N·4D".
  - Verificado por screenshots: Fez 2n3d (BC + 3 peers), Fez-Sidiali 3n4d/4n5d (BC + peers bajo Fez), Marrakech 2n3d (BC + 1 peer rakAgafay), Desierto/Atlas (BC directo, sin peer-nav), intros Fez/Marrakech/Tánger (BC de hub). Frontend compila.

- **Fases 3 y 4 — Navegación & enlaces "Escapadas cortas" (`tourShort`) y "Norte de Marruecos" (`tourNorth`) (jun 2026) — COMPLETADO + VERIFICADO (self-test, screenshots)**:
  - Registrados en `lib/programNav.js#SECTION_HUBS` (importados de `itineraryHubs.js`) sin solapamientos:
    - Fase 4 · `tourNorth`: `HUB_NORTE_CIUDADES_IMPERIALES` (`tourNorteCiudadesImperiales`) + `HUB_NORTE_TANGER_FEZ` (`tourNorteTangerFez`).
    - Fase 3 · `tourShort`: `HUB_ESCAPADA_RAK_ERG_RAK` (`tourEscapadaRakErgRakHub`).
  - Esto auto-construye `PROGRAM_NAV` → en cada página de programa de esas secciones se activan **Breadcrumbs** (`Inicio › Viajes › <Sección> › <Hub> › <Programa>`, con enlaces a `/viajes`, sección y hub) y **HubPeerNav** ("otros programas de este hub").
  - Verificado por screenshots: Norte → `INICIO › VIAJES › NORTE DE MARRUECOS › CIUDADES IMPERIALES › 4 NOCHES · 5 DÍAS`; Escapada → `INICIO › VIAJES › ESCAPADAS CORTAS › ESCAPADAS… › 2 NOCHES · 3 DÍAS`; HubPeerNav (`hub-peer-nav`) presente en ambas; frontend compila.
  - ALCANCE: solo existe 1 hub de escapadas (`HUB_ESCAPADA_RAK_ERG_RAK`). El resto de escapadas con ruta propia (Fez, Marrakech, Fez–Sidiali, Desierto, Atlas, Rak–Agafay, Tánger) NO tienen objeto "hub" todavía → sin breadcrumbs/peer-nav hasta crearlos (backlog).
- **Cierre del bucle de navegación + prefetch del héroe (jun 2026) — COMPLETADO + VERIFICADO (self-test)**:
  - Hallazgo: el enlazado sección→hub→programa→hermanos YA estaba completo — las variantes de la pasarela `/viajes/marruecos` (`itinerary-variant-link-*`) deep-linkan al programa (`pathFor(lang, v.link)`), las tarjetas de los hubs (`hub-program-*`) también (`p.link`), el héroe del programa (`ProgramHero`/`EditableImage priority`) ya usa eager + `fetchPriority=high`, y los breadcrumbs/HubPeerNav se activaron en la Fase 2.
  - Añadido: `warmTripHero(routeId)` en `lib/tripHero.js` — prefetch del héroe del programa destino en `onMouseEnter`/`onFocus` de las tarjetas de variantes (`JourneyPageSections`) y de hub (`ItineraryHubPage`), al ancho que el navegador realmente pedirá para un héroe 100vw (viewport×DPR, snap a bucket responsive). Calienta caché de navegador Y servidor (anchos grandes que el warm-up batch no cubre) antes del clic → transición instantánea.
  - Verificado: hover sobre una variante dispara exactamente 1 petición al héroe correcto del programa (unsplash, w=1920); tarjetas de hub enlazan al programa; breadcrumbs OK; frontend compila.
- **Fase 2 — Navegación & enlaces "Marruecos de norte a sur" (`tourFull`) (jun 2026) — COMPLETADO + VERIFICADO (self-test)**:
  - Registrados en `lib/programNav.js#SECTION_HUBS` los 4 hubs primarios de Gran Sur (importados de `itineraryHubs.js`): `HUB_GRANSUR_FEZ_RAK` (`tourGransurFezRak`), `HUB_GRANSUR_FEZ_SIDIALI_RAK` (`tourGransurFezSidiali`), `HUB_GRANSUR_OUARZA_FEZ` (`tourGransurOuarzaFez`), `HUB_GRANSUR_TANGER_RAK` (`tourGransurTangerRak`). Esto auto-construye `PROGRAM_NAV` → activa en CADA página de programa de la sección: **Breadcrumbs** (`Inicio › Viajes › Marruecos de norte a sur › <Hub> › <Programa>`, con enlaces a `/viajes/marruecos` y al hub) y **HubPeerNav** ("otros programas de este hub").
  - Verificado por screenshots en 3 hubs: Fez–Marrakech (8 hermanos), Tánger–Marrakech, Ouarza/Alto Atlas–Desierto–Fez. Breadcrumbs y peer-nav correctos.
  - Decisión de alcance: NO se registró `HUB_ATLAS_DESIERTO_FEZ` (empaqueta programas de Escapadas/Aventura → se reclamarían antes de tiempo; gana el primero registrado). `HUB_GRANSUR_TANGER_RAK` incluye algunos programas del Norte (Ciudades Imperiales, Tánger–Fez) que ahora muestran breadcrumb "Marruecos de norte a sur" de forma INTERINA (antes no tenían ninguno); las Fases 3 (Escapadas) y 4 (Norte) afinarán la propiedad de esos programas.
- **Warm-up de caché de imágenes para TODAS las imágenes (jun 2026) — COMPLETADO + TESTEADO (iteration_49 + self-test)**:
  - Objetivo: que incluso la primera visita "fría" cargue al instante (sin coste de codificación AVIF en el primer request) para todas las imágenes.
  - Backend: pre-genera variantes AVIF+WebP (anchos `WARM_WIDTHS=[24,768,960]` — LQIP + los anchos que eligen las galerías de día) de cada imagen en `db.files`, escribiéndolas en la caché de disco (`/app/backend/img_cache`). Usa un **ProcessPoolExecutor** (contexto `spawn`, `cpu-2` workers) con `_encode_all_variants` (decodifica la original una vez, 1 envío IPC por imagen) para paralelizar el encode CPU-bound. Idempotente (salta variantes ya cacheadas). Se lanza solo en background al arrancar (delay 20s) y bajo demanda. Endpoints `POST/GET /api/admin/warm-image-cache[/status]` (Bearer admin). AVIF `speed=8`.
  - **Auto-warm tras subir/editar imagen (jun 2026)**: helper `_warm_one_path(path, data)` fire-and-forget enganchado tras `put_object` en los 4 endpoints de subida (`/slots/{id}/upload`, `/day-galleries/{key}/upload`, `/library/upload`, `/files/{id}/replace`). Reutiliza los bytes ya optimizados (sin round-trip a storage) y calienta las variantes en un thread. Verificado por self-test: una subida real dejó las 6 variantes (24/768/960 × avif/webp) CACHED en ~3s sin pulsar el botón.
  - Frontend: panel `WarmCachePanel` en la pestaña "Mirror DB" del admin ("Optimizar todas las imágenes") con botón + barra de progreso en vivo (polling cada 1.5s). FIX (iteration_49): el polling se liga a `status.running` vía useEffect, de modo que el progreso se actualiza tanto si lo lanza el botón como si el warm-up ya venía corriendo desde el arranque (botón deshabilitado). Auto-verificado: el progreso avanza solo sin interacción.
  - Resultado: variantes servidas desde caché en ~5ms (AVIF 8.6KB) / LQIP 365B en ~1.6ms. Cubre LQIP + galerías de día; héroes/retina (eager, pocos) se codifican bajo demanda y quedan cacheados. Nota: la caché de disco es efímera (se regenera tras un redeploy; el warm-up se relanza solo al arrancar).
- **Blur-up (LQIP) en galerías de día — fin del placeholder negro (jun 2026) — COMPLETADO + TESTEADO (100%, iteration_48)**:
  - BUG: en carga "fría" las imágenes de día tardaban (fetch del original + encode AVIF) y mientras tanto el contenedor `bg-[#1A1513]` mostraba NEGRO. Las galerías dinámicas usaban `<Img>` (un `<img>` desnudo sin placeholder), de ahí el negro; `EditableImage` ya tenía shimmer cálido.
  - FIX (blur-up): nuevo helper `lib/imageUrl.js#lqipSrc(url)` = `optimizedSrc(url, 24)` → miniatura LQIP diminuta (~365 bytes AVIF). `Img.jsx` ahora es stateful: mientras carga aplica la clase `.img-blurup` + `backgroundImage` inline con el LQIP (desenfocado por CSS), y al `onLoad` añade `is-loaded` (quita el blur y el background). `EditableImage`/SmartImage usa el mismo LQIP sobre su `cms-img-fade` (sustituye el shimmer cuando la fuente es optimizable). CSS `.img-blurup` en `index.css`: `blur(16px)`→`blur(0)`, fallback `background-color:#2b2622` (marrón cálido, nunca negro). Backend `download_file`: buckets de ancho ahora incluyen **24 y 48** para servir LQIP reales (~24px).
  - Resultado: la imagen aparece al instante como preview suave y se nitidiza progresivamente; sin negro, sin CLS (el visor reserva su aspect-ratio), lazy bajo el fold + eager/`fetchpriority=high` en `priority`. Verificado por testing agent bajo throttling 250KB/s en 2 itinerarios: visor + miniaturas con blur-up, 0 imágenes rotas, navegación next/prev conserva el efecto.
- **Pipeline de imágenes optimizado en TODA la web (jun 2026) — COMPLETADO + TESTEADO (100%, iteration_46 + iteration_47, 0 imágenes rotas)**:
  - Componente envoltorio `components/Img.jsx` (drop-in de `<img>` para imágenes NO editables: carruseles, galerías, catálogos, miniaturas de admin/pickers). Enruta el `src` por el pipeline responsive/moderno vía `lib/imageUrl.js`: `srcSet` (320..1920) + `sizes`, `src` optimizado (cap de ancho), `loading=lazy` (eager+fetchpriority=high si `priority`), `decoding=async`. Solo optimiza Unsplash, Pexels y el proxy `/api/files` (`?w=&fmt=auto`); URLs no optimizables (svg/gif/data/locales) pasan intactas.
  - Migrados ~16 `<img>` crudos → `<Img>`: CONTENIDO (`TripPostcards` sello, `ToursVideoSection` poster, `MoroccoIntroVideo` poster, `StubPage` héroe `priority`, `DayImageGallery` slide dinámico) + ADMIN/PICKERS (`GalleryManager` x2, `LibraryManager`, `PexelsTab`, `UnsplashTab`, `PexelsSelectionTab`, `PexelsImageSearch` x2, `ImageLibraryPicker`, `AdminPage` x3). Se mantienen `<img>` crudo a propósito: logos/monogramas locales (webp/svg bundled) y previews de `ImageEditorPage`. `EditableImage`/SmartImage (grueso del contenido editable) ya usaba `optimizedSrc`+`buildSrcSet`+`sizes`+lazy/priority. No hay fondos fotográficos en CSS (solo patrones SVG/fuentes).
  - **Backend `/api/files` elevado al máximo estándar (afecta a TODAS las imágenes proxied)**: `?fmt=auto` ahora **prefiere AVIF** cuando el navegador lo soporta (Accept), si no WebP, si no original redimensionado. Calidad por formato: **AVIF q58/speed6** (~30% más ligero que WebP a igual percepción) y **WebP q80/method6**. El encode CPU-bound se movió a un **worker thread** (`asyncio.to_thread`, helper `_encode_variant`) para no bloquear el event loop bajo cargas de AVIF; variantes content-addressed en caché de disco (`/app/backend/img_cache`) con `Cache-Control: immutable`. Verificado por curl: misma imagen w=960 → AVIF 8.6KB vs WebP 10.9KB vs JPEG 27.5KB. (Nota: el ingress de PREVIEW sobreescribe Cache-Control a no-store; en producción se respeta el immutable.)
  - Verificado por testing agent (iteration_47): 0 imágenes rotas en home/catálogo/itinerario/admin (editor Galerías + Biblioteca + iframe preview); negociación AVIF transparente al frontend; carruseles navegan; sin regresión visual. Pre-existente fuera de alcance: warning de clave duplicada de React en LibraryManager (RCA iteration_45).
- **Library — biblioteca de medios de destinos en /admin (jun 2026) — COMPLETADO + TESTEADO (100%, 10/10 flujos)**:
  - Nueva pestaña "Library" en `/admin` (`components/LibraryManager.jsx`, icono Library, entre Galerías y Leads). Indexa automáticamente las ~128 localizaciones de TODOS los itinerarios (`lib/libraryIndex.js` → `buildLibraryIndex` desde `LANDMARK_CATALOG` + `tripsForPoi`, deduplicado por id).
  - **INDEPENDIENTE de las galerías curadas**: colección propia `library_locations` (no toca `image_slots`/`poi.*.gallery.*`). Verificado: `image_slots` intacto (1032) tras editar el Library. Es una fuente SECUNDARIA de imágenes contextuales.
  - Por lugar: nombre (ES/EN/FR), tipo (`kind`: ciudad/aldea/kasbah/hotel/dunas/valle/gargantas/mirador/mercado/palmeral/sitio/fósiles/montaña/campamento/aeropuerto), zona, **galería propia** (sembrada con la galería por defecto del catálogo), captions por imagen, notas/metadatos, y **lista de itinerarios que lo referencian** con enlaces.
  - Búsqueda + filtros por tipo y por zona; añadir imágenes vía `ImageLibraryPicker` (biblioteca/Pexels, multi-select); **botón "Sugerir imágenes"** por lugar que abre el picker en la pestaña Pexels con la búsqueda pre-rellenada por el nombre del destino + "Marruecos" (props nuevas `initialTab`/`initialQuery` en ImageLibraryPicker y PexelsTab); quitar; guardar (persistencia verificada); botón Re-indexar.
  - Backend (`server.py`): `GET /api/library/locations`, `POST /api/library/locations/sync` (upsert metadata; siembra imágenes solo en nuevos, idempotente), `PUT /api/library/locations/{id}` (escribe solo en `library_locations`). (testing iteration_45, 100%).
  - PENDIENTE (fase 2, según el usuario): modal "Ver más imágenes relacionadas" dentro de la galería del itinerario que tire del Library; fusión manual de lugares; recomendaciones de imágenes con IA (LLM). [Hecho ya: sugerencias por Pexels por nombre del lugar.]

- **Buscador de textos en /admin → pestaña "Textos" + REGISTRO CENTRAL (jun 2026) — COMPLETADO + TESTEADO**:
  - Nuevo componente `components/TextSlotsPanel.jsx` y nueva pestaña "Textos" en `AdminPage.jsx` (entre URLs y Galerías).
  - **Registro central de slots**: `EditableText` reporta (batch, fire-and-forget, deduplicado por sesión) cada `slot`+`defaults` a `POST /api/text_slots/register`; backend los guarda en colección `text_slot_registry`. El panel hace merge de `GET /api/text_slots` (valores guardados) + `GET /api/text_slots/registry` (defaults) → lista el **100% de textos editables**, también los nunca editados (badge "sin editar", defaults precargados). Tras navegar el sitio el registro creció a 484+ slots (576 textos visibles en el panel).
  - Búsqueda full-text por id de slot y por contenido ES/EN/FR; filtro "Solo sin editar"; edición trilingüe inline; Guardar (`PUT /api/text_slots/{id}`) con persistencia verificada; autotraducción ES→EN/FR (`POST /api/translate`); enlace "Ver" por página.
  - IMPORTANTE rutas backend: `/text_slots/registry` y `/text_slots/register` declaradas ANTES de `/text_slots/{slot_id}` para no colisionar con el path param.
  - Nota: el registro se rellena a medida que se navegan las páginas (en producción crecerá con el tráfico; en preview se puede sembrar visitando las páginas o desde el iframe de preview del admin).


- **Modo edición de TEXTOS: navegar slides mientras editas + cobertura de textos (jun 2026) — FASE A + B1 + B2/B3 parcial — COMPLETADO + TESTEADO (100%)**:
  - **Fase A (navegar slides editando texto)**: `EditModeContext` ahora expone `anyEditMode` (imagen||texto). Los carruseles con autoplay/fade (`EmotionalIntro`, `CategoryImageCarousel`) PAUSAN el autoplay y MUESTRAN flechas/puntos también en modo TEXTO (antes solo en modo imagen) → se puede ir slide a slide y editar el texto de cada uno. Pies de foto de `EmotionalIntro` ahora editables por slide: `home.intro.caption.${idx}`. (testing iteration_43, 100%).
  - **B1 (textos del Home)**: HeroSlider `home.hero.place`; TravelCategories badges `home.cat.<slug>.badge.<b>`, región `home.cat.<slug>.region`, `home.cat.options_label`, `home.cat.discover_cta`; tarjetas de `AllTripsCarousel` y `HomeCategoryCarousel` editables y SINCRONIZADAS por viaje vía `tripTextSlot(routeId, field)` = `trip.${routeId}.{tag|duration|title|summary|route}` (helper en `lib/tripHero.js`; rutas agregadas usan `home.cat-carousel.${id}.*`); labels `home.alltrips.card_cta`, `home.trip_card.{from|spots|view}`, `${slotPrefix}.view_all`.
  - **B2 (chrome global)**: `Footer` 100% editable (`footer.brand/tag/cta/explore_label/link.<k>/contact_label/address.line{1,2}/office_hours_{label,value}/rights/contact_24_7`). `SideMenu` (menú lateral) con TODOS los ítems editables: `menu.<l0id>.label`, `menu.<l0id>.header`, `menu.group.<gid>.label`, `menu.item.<itemid>.label`; reusa `footer.brand/contact_label/office_hours_value` (single source).
  - **B3 parcial (formularios)**: `FormExtras` (compartido por PlannerForm/ContactForm) editable: `form.next.title/step.N/response`, `form.pref.label/hint/option.{phone,email}`.
  - Defaults trilingües; autotraducción ES→EN/FR al editar (elección usuario). Navegación bloqueada al editar (links no navegan; el texto editable sí). (testing iteration_44, 100%).
  - **CAVEAT menú**: el botón hamburguesa (`header-menu-button`) lleva su label "MENÚ" editable; en modo texto, hay que pulsar el ICONO para ABRIR el menú (pulsar el texto edita el label). Pendiente: decidir si priorizar apertura del menú sobre edición de ese label.
  - **PENDIENTE (backlog de cobertura total)**: campos de formularios (PlannerForm/ContactForm labels ya editables; faltan PLACEHOLDERS → requieren mecanismo aparte, no se pueden envolver con EditableText), y componentes/páginas secundarias (JourneyPageSections, HomeAllTripsCatalog, Testimonials, TravelFaq, TripPostcards, PricingSection, DownloadProgramModal, Breadcrumbs, BestMonthFab, departures de TravelCategories, TopInfoBar, etc.).

- **Mirror Production Database (pestaña /admin → "Mirror DB") (jun 2026) — COMPLETADO + TESTEADO**: Botón que copia TODO el contenido de producción (`https://xalucatravel.com`) al entorno actual (preview) y lo sobrescribe, para que coincidan antes de un despliegue (evita perder ediciones de CMS/Edit Text/Edit Image hechas en vivo). Backend `POST /api/admin/mirror-production` (Bearer admin): lee SOLO los GET públicos que producción YA expone (sin redeploy) — `/api/cms/export` (image_slots, text_slots, pricing), `/api/day-galleries`, `/api/files` (paginado) — y sobrescribe `db.image_slots`, `db.text_slots`, `db.day_galleries`, `db.files`, `db.config(pricing)`. Hace fetch completo ANTES de borrar (si falla la red, no toca la BD local). URLs relativizadas con `_relativize_url`. Los binarios viven en object storage COMPARTIDO (clave EMERGENT_LLM_KEY, app "xaluca") → las imágenes resuelven al instante sin copiar ficheros. EXCLUYE leads/PII (contact_requests, trip_planner_requests, downloads). Origen configurable vía env `PRODUCTION_BASE_URL` (def. xalucatravel.com). Frontend: pestaña "Mirror DB" + `MirrorPanel` (confirmación destructiva, estado de carga, panel de resultados con conteos). Verificado por testing agent (iteration_42, 100%): mirror real ejecutado → 98 textos / 1032 imágenes / 457 galerías / 4035 archivos / precios=Sí; home pública carga OK tras el mirror; 0 errores. NOTA: ejecutar en PREVIEW antes de desplegar; no modifica producción (solo lee). Backlog: extraer MirrorPanel a su propio archivo (refactor AdminPage) y renderizar conteos dinámicamente.
- **SEO + Open Graph / Twitter en toda la web (jun 2026) — COMPLETADO + VERIFICADO (curl)**: Etiquetas sociales ESTÁTICAS en `public/index.html` (lo único que leen WhatsApp/Facebook/LinkedIn/Telegram/iMessage/X, sin JS): `lang=es`, `<title>`, meta description, canonical (`https://xalucatravel.com/`), OG completo (type, site_name, title, description, url, image + image:secure_url/type/width=1200/height=630/alt, locale es_ES + alternates en/fr), Twitter `summary_large_image` (title/description/image/alt), theme-color `#1A1513`. Imagen OG de marca **1200×630** generada con Pillow en `public/og-image.jpg` (fondo #1A1513, patrón bereber dorado dibujado, "Xaluca Tours" serif + tagline "Viajes a medida por Marruecos" + dominio) — 127 KB, servida 200 en `/og-image.jpg`. SEO dinámico por página: `lib/seoMeta.js` (mapa trilingüe por routeId + fallback de marca) consumido por `<SeoHead>` cableado en `LocalizedRouter` (App.js) → título/description/canonical/hreflang(es/en/fr/x-default)/OG por página para Google (que sí renderiza JS). Verificado: HTML servido contiene las etiquetas correctas y la imagen carga; home renderiza OK. NOTA: las URLs absolutas usan `xalucatravel.com` (elección del usuario) → los previews sociales funcionarán tras desplegar a ese dominio + DNS; validar luego con Facebook Sharing Debugger / X Card Validator / LinkedIn Post Inspector.
- **Selección múltiple también en Pexels y Selección (Galerías /admin) (jun 2026) — COMPLETADO + TESTEADO**: La multiselección de la pestaña "Biblioteca" se extendió a las pestañas "Pexels" y "Selección". El estado de selección se elevó a `ImageLibraryPicker` (`selectionMode` activo en library|pexels|pexels-selection; `confirmSelection` ahora es async). En Pexels/Selección, al pulsar una foto se ALTERNA selección (check + ring) sin importar; al pulsar "Añadir N imágenes" se descargan todas al storage (`POST /api/pexels/import`, con estado "Añadiendo…", tolerante a fallos por foto) y se insertan en una sola acción (`onSelectMany` → único PUT `/day-galleries/<key>`). UX/indicadores/contador/footer idénticos en las 3 pestañas; cambiar de pestaña resetea la selección (no mezcla items hospedados con los pendientes de importar). Unsplash se mantiene de selección simple. Verificado por testing agent (iteration_40, 100% 14/14: toggle/deselección, contador, loading, delta exacto +2 por pestaña, reset al cambiar pestaña, regresión Biblioteca, 0 errores consola; 6 imágenes añadidas y luego eliminadas en limpieza).
- **"Notas del día" replicadas a TODOS los viajes (auto-derivadas + top-up práctico) (jun 2026) — COMPLETADO + TESTEADO**: El carrusel post-it por DÍA (`DayTravelNotes`, bajo la galería de cada día) ya aparece en TODOS los programas. `dayTravelNotes.js` auto-deriva 3 notas por día desde el array `culture` (curado, trilingüe, lugares reales) de cada día vía `TRIP_PROGRAMS`; si un día tiene <3 bloques culture (o ninguno), se completa hasta 3 con consejos prácticos por temática (`THEME_TIPS`: desert/mountain/city/coast/general) detectada de title+body (`detectTheme`, prioriza ciudad antes que montaña), con rotación por día para no repetir. Días sin culture lideran con su propia descripción + 2 tips. El bespoke de `tourAtlasDesierto67` mantiene prioridad. `tidyTitle` (corta en ":" temprano) y `firstSentences` (≤2 frases) mantienen el post-it compacto. Cada nota es editable por CMS con slots únicos por ruta+día (`program.${routeId}.day.${dayIndex}.note.${i}.{tagline|title|body}`) → independientes por viaje. Verificado por testing agent (iteration_39, 100%: fez_tanger 7 días×3 notas incl. llegada/regreso; marrakech_ergchebbi 3 notas culturales sin top-up; atlas_desierto bespoke intacto; CMS editable; 0 errores consola). Nota: Chatbase auto-abre cerca del fondo y puede interceptar clics en pruebas.
- **Notas de viaje bajo el mapa: editables por CMS e independientes por viaje (jun 2026) — COMPLETADO + TESTEADO**: La sección post-it "Prepara tu maleta" (`TripPackingNotes`, bajo el mapa "El recorrido completo / Tu travesía en un solo mapa", columna izquierda) ya aparece en TODAS las páginas de viaje (getTripPackingNotes siempre devuelve 3 notas por perfil; fallback SOUTH_DESERT). NUEVO: cada nota (tagline + título + cada ítem de la lista) es ahora editable inline vía `<EditableText>` con slots ÚNICOS por viaje: `trip.${routeId}.packing.note.${i}.{tagline|title|item.${j}}`. El contenido por perfil sigue siendo el DEFAULT (adaptado al tipo de itinerario: norte=ciudades/medinas, sur=Atlas/dunas, costa, aventura, escapadas). Cada página guarda sus notas de forma independiente (editar un viaje NO afecta a otro). Firma "— Tu equipo Xaluca" se mantiene estática. Verificado por testing agent (iteration_37, 100%: render en atlas_desierto/desierto_atlas/fez_tanger, defaults adaptados norte vs sur, contenteditable con testids por slot, PUT /api/text_slots persistente tras recarga, independencia entre viajes, trilingüe es/en/fr, carrusel flechas+dots). Datos de prueba restaurados.
- **Selección múltiple en la Biblioteca de imágenes (Galerías /admin) (jun 2026) — COMPLETADO + TESTEADO**: La pestaña "Biblioteca" del `ImageLibraryPicker` ahora permite seleccionar VARIAS imágenes a la vez y añadirlas al día del itinerario en una sola acción. Nuevos props `multiple` + `onSelectMany` (solo activos en la pestaña local "Biblioteca"; Pexels/Unsplash/Selección conservan el pick individual). `selectedMap` (Map id→item, conserva orden de selección); clic en miniatura alterna selección (check + ring), botón footer "Añadir N imágenes" (`image-library-add-selected`) + "Limpiar" (`image-library-clear-selection`); la selección se resetea al cerrar el modal o cambiar de pestaña. `GalleryManager.onPickManyFromLibrary` añade todas en orden y persiste con un único PUT `/day-galleries/<key>`, luego cierra. Verificado por testing agent (iteration_36, 14/14 funcionales: toggle/deselect/limpiar, delta de contador = nº seleccionado, persistencia tras recarga, regresión single-pick en Pexels/Unsplash/Selección). i18n corregido (plural "imágenes" con tilde).
- **Notas de viaje por DÍA (post-it editables por CMS) en el itinerario (jun 2026) — COMPLETADO + TESTEADO**: Nuevo componente `DayTravelNotes.jsx` — carrusel deslizante de 3 post-its independientes DEBAJO de la galería de imágenes de CADA día (columna izquierda de `DayBlock` en `ProgramTemplate.jsx`). Cada nota (etiqueta/título/cuerpo) es editable inline vía `<EditableText>` con slots únicos por viaje+día indexado: `program.${routeId}.day.${dayIndex}.note.${i}.{tagline|title|body}` (índice 1-based, no el dayId, para que no haya fugas entre días que compartan id). Contenido por defecto trilingüe (es/en/fr) en `lib/dayTravelNotes.js` (días 1..7). Visual coherente con las notas del mapa (papel envejecido, washi tape, chincheta, `font-hand`); icono/acento fijos por posición (Star/Lightbulb/Camera). Gating: `getDayTravelNotes(routeId, dayIndex)` → solo se muestra en `tourAtlasDesierto67` (`/viajes/atlas_desierto/programa_6n_7d`); devuelve null en el resto. Carrusel con flechas (`day-notes-prev/next-<dayId>`) y dots (`day-notes-dot-<dayId>-i`); `onScroll` calcula la nota activa por el CENTRO del viewport (corrige desync de dots en columna estrecha). Verificado por testing agent (iteration_35, 14/14 funcionales: 7 bloques, 3 notas c/u, contenido ES día 1, switch es/en/fr, contenteditable on/off, caso negativo en marrakech_ergchebbi=0 bloques, sin errores de consola).
- **Herramienta de migración de galerías a clave definitiva (jun 2026) — COMPLETADO + TESTEADO**: Botón "Migrar galerías a clave definitiva" en el header del Gallery Dashboard (`GalleryManager`, `data-testid=gallery-migrate-btn`). `runMigration()` (frontend, usa `TRIP_PROGRAMS`+`namespaceForRouteId`): Fase 1 copia cada galería de la clave antigua `${ns}.day.${id}` a la nueva indexada `${ns}.day.${index}.${id}` (a TODOS los días que comparten id en programas con colisión → opción usuario 1a), saltando las ya migradas; Fase 2 limpia las claves antiguas consumidas (solo tras copiarlas). Idempotente y seguro; muestra resumen (Migradas/Omitidas/Limpiadas/Errores). El fallback retrocompatible se MANTIENE como red de seguridad (opción usuario 2a). Verificado en preview: 1ª ejecución migró 1/limpió 1, 2ª ejecución 0/0 (idempotente); resultado: todas las claves quedan en el esquema indexado definitivo. REQUIERE ejecutarse en PRODUCCIÓN tras redesplegar (el botón actúa sobre la BD del entorno donde se abre el admin).
- **Recuperación retrocompatible de galerías tras el cambio de clave (jun 2026) — COMPLETADO + TESTEADO**: El cambio anterior a clave indexada `${ns}.day.${index}.${dayId}` provocó que en PRODUCCIÓN las galerías guardadas con la clave antigua `${ns}.day.${dayId}` dejaran de mostrarse (solo aparecía 1 imagen/día por fallback). Los DATOS NO se perdieron (siguen en la BD bajo la clave antigua). FIX retrocompatible: `DayImageGallery` lee la clave nueva y, si está vacía, recurre a la antigua (`base`); `GalleryManager.seedFor` recupera también `galleries[legacyBase]`. Al re-guardar un día, migra a la clave indexada. Para días con `day.id` duplicado, el fallback puede mostrar la galería antigua compartida hasta el primer re-guardado (comportamiento previo, aceptable). Verificado en preview: día `dia-final` (galería legacy de 2 imgs) recuperado tanto en público (contador 1/2) como en admin (2 thumbs). REQUIERE REDESPLIEGUE para aplicar a producción.
- **Aislamiento de galerías por día en el Gallery Dashboard /admin (jun 2026) — COMPLETADO + TESTEADO**: BUG raíz: varias programas (PROGRAM_ME_67=tourMarrakechErg67, MEM_56/67/78, MES_67, FAE_56) tienen dos días con el MISMO `day.id` (p.ej. dos "dia-2"), por lo que la clave de galería `${ns}.day.${dayId}` colisionaba → imágenes compartidas entre días + claves React duplicadas. FIX: clave por POSICIÓN del día `${ns}.day.${index}.${dayId}` (helper `dayGallerySegment` en `lib/dayGalleryStore.js`), idéntica en admin (`GalleryManager.apiKey`) y público (`DayImageGallery` vía `ProgramTemplate dayIndex={idx+1}`). Los slots inline legacy siguen siendo id-based (fallback/seed). Además: (1) `onUpload` ahora persiste primero el seed para que la 1ª subida AÑADA en vez de reemplazar las imágenes sembradas; (2) el modal de Biblioteca se cierra automáticamente tras elegir imagen. Verificado por testing agent (iteration_34, 100% en aislamiento/optimista/cambio de viaje sobre tourMarrakechErg67) + self-test (seed preservado 1→2, modal auto-cierra).
- **Notas de viaje (post-it) replicadas a TODOS los viajes por perfil (jun 2026) — COMPLETADO**: `lib/tripPackingNotes.js` reestructurado con 8 PERFILES de equipaje (SOUTH_DESERT, FULL_CROSSING, NORTH, COAST, ADVENTURE, SHORT_DESERT, SHORT_ATLAS, SHORT_CITY) + clasificador `classify(routeId)` que mapea cada uno de los ~67 programas a su perfil según región/estilo (enduro→aventura, ciudades/rif/tánger→norte, marrakech+fez/desierto→travesía, essaouira→costa, escapadas→corta desierto/atlas/ciudad, sur/desierto→sur). `getTripPackingNotes(routeId)` resuelve override→perfil. Contenido trilingüe adaptado por perfil. `ROUTE_OVERRIDES` permite notas bespoke por viaje si se desea. Iconos por tema añadidos en `TripPackingNotes.jsx` (Footprints/Wind/Mountain/Backpack). Verificado en 5 perfiles (norte, travesía, costa, aventura, escapada) — títulos correctos y 3 notas cada uno.
- **Notas de viaje (post-it) bajo el mapa "El recorrido completo" (jun 2026) — COMPLETADO (piloto)**: Nuevo carrusel deslizante de notas adhesivas/post-it ("Prepara tu maleta") en la columna izquierda de `TripRouteMap`, debajo del mapa y la leyenda. Estilo reinterpretado de las postales (papel envejecido, washi tape, chincheta, tipografía manuscrita `font-hand`), con 3 notas sobre qué llevar en la maleta (ropa por capas, accesorios, noches en el desierto). Componente `TripPackingNotes.jsx` + datos trilingües en `lib/tripPackingNotes.js` (con `getTripPackingNotes(routeId)`). De momento SOLO en `tourAtlasDesierto67` (`/viajes/atlas_desierto/programa_6n_7d`); el resto de viajes no muestra nada hasta replicar contenido. `TripRouteMap` ahora recibe `routeId` desde `ProgramTemplate`. Verificado por screenshot + flechas/dots.
- **Grupo CTA de 5 iconos unificado en TODAS las tarjetas de viaje (jun 2026) — COMPLETADO**: Componente reutilizable `TripCardActions.jsx` (Planificar/brújula · Asistente · Llamar · Cita previa · Compartir) con variantes `size` (sm/md) y `tone` (light/dark). Aplicado debajo del CTA en: `AllTripsCarousel`, `HomeAllTripsCatalog`, `HomeCategoryCarousel` ("Ver viaje"), y las tarjetas hub overlay de `JourneyPageSections` (`HubOptionsPreview`, tone dark). Patrón clave: las tarjetas dejan de envolver los botones dentro del `<Link>` (en overlay se usa un `<Link absolute inset-0>` + overlay `pointer-events-none` y acciones `pointer-events-auto`). `ShareTripButton` acepta `shareUrl`/`testid`/`triggerClassName`/`iconClassName`. ToursRegionMap excluido (links de texto/panel de región, no tarjetas). Tarjetas de categoría/editorial (OurTrips, TravelCategories) excluidas por decisión del usuario. Verificado por interacción en home + página Sur (share abre diálogo sin navegar, plan→/planifica-tu-viaje, tarjeta→ficha del programa).
- **Botones CTA (5 iconos) en HomeAllTripsCatalog (jun 2026) — COMPLETADO**: Mismo set de 5 iconos (Planificar/brújula, Asistente, Llamar, Cita previa, Compartir) en cada tarjeta de "Cada ruta, en detalle / N viajes disponibles", colocados justo debajo de "Ver itinerario". Refactor: tarjeta ya no envuelta entera en `<Link>` (evita navegación nativa del ancla); `<Link>` envuelve imagen+contenido y los botones son hermanos. Verificado por interacción.
- **Botones CTA (5 iconos) en AllTripsCarousel (jun 2026) — COMPLETADO**: Grupo de 5 iconos (Planificar/brújula, Asistente/Chatbase, Llamar, Cita previa, Compartir) en cada tarjeta de "Todos los viajes" de la home, consistente con LandmarkCarousel. Refactor clave: la tarjeta ya NO está envuelta entera en `<Link>` (causaba navegación nativa del ancla al pulsar los botones anidados); ahora `<Link>` envuelve solo imagen+descripción y los botones son hermanos. `ShareTripButton` acepta props `shareUrl` (URL del viaje concreto) y `testid`. Verificado por screenshot+interacción (share abre diálogo sin navegar; Planificar→/planifica-tu-viaje; Cita previa→/citaprevia; tarjeta→ficha del viaje).

- **Galería visual por día (jun 2026) — COMPLETADO**: En las páginas de programa (`ProgramTemplate.jsx` → `DayBlock`), cada día tiene una galería de **10 imágenes** (1 principal 5/6 + 9 cuadradas 1:1). `DayImageGallery.jsx`: visor grande + carrusel horizontal de miniaturas cuadradas (72/84px) con scroll fluido, alineado al ancho exacto del visor (w-full dentro del wrapper sticky); click en miniatura→principal, flechas prev/next recorren las 10, miniatura activa resaltada + auto-scroll del rail, swipe en visor (touch) y en rail (scroll nativo), contador "n/10". Cada imagen es slot CMS (`day.<id>.image` + `day.<id>.slide.0..8`) dentro de `<EditableGroup>` → editor con subida múltiple. `EXTRA_COUNT` configurable. Verificado por screenshot + interacción.
- **Canal de contacto preferido + "¿Qué sucede después?" (jun 2026) — COMPLETADO**: PlannerForm y ContactForm con bloque "¿Qué sucede después?" y campo obligatorio multi-selección "¿Cómo prefieres que te contactemos?" (📞/✉️). `FormExtras.jsx`. Backend `preferred_contact: List[str]` (coerción str→[str]) + email. Admin: columna "Canal pref.". Verificado (testing agent iteration_30 + curl).
- **Asistente Virtual (Chatbase) ubicuo (jun 2026)**: botones/iconos en TravelCategories, AllTripsCarousel, HomeCategoryCarousel, HubOptionsPreview, DayBlock (con "Contactar" → /planifica-tu-viaje?trip=routeId que pre-rellena el planner). Pestaña "Asistente Virtual" y "Cita previa" (componente reutilizable `BookingSession.jsx`) en `FormTabs` (`Elige cómo contactarnos`). Marca X reposicionada a top-left en HubOptionsPreview para evitar conflicto con el icono de asistente.
- **/precios ampliado (P0) — COMPLETADO**: Sección "Todos nuestros viajes y sus precios". Recorre ALL_TRIPS agrupado por región (TRIP_REGIONS). Cada viaje: nombre (enlace a su itinerario vía pathFor), región, ritmo, nº noches, precio "Desde" y tabla por persona (tramos 2-4 pax existentes) en temporada baja/alta. Usa getProgramTiers() o pricing global/admin. Todo CMS-editable (EditableText). Verificado por screenshot.
- CMS global, catálogo de viajes, Leads Dashboard, audio global, emails Resend con imágenes branded, flipbook Publuu Dark Academia, gestión de emails de notificación en Admin.

## Backlog / Próximas tareas
- ✅ HECHO: Phase 2/3/4 Navegación/Linking (Gran Sur, Escapadas Rak-Erg-Rak, Norte)
- ✅ HECHO: Hubs de Escapadas (Fez, Marrakech, Tánger) + programas directos (Desierto, Atlas) cableados en `tourShort`. Fez-Sidiali agrupado bajo hub Fez.
- P2: Aliases POI no mapeados en textos narrativos (lib/programs/, dayPlaceGazetteer.js)
- P2: Refactor server.py (>3200 líneas) → routers; dividir AdminPage.jsx (>2400 líneas)
- P2: Aliases/redirects para slugs inconsistentes (evitar 404)
- P3: Stripe Checkout en /proximas_salidas
- P3: Integrar Library en galería curada ("Ver más imágenes relacionadas" desde library_locations)

## Credenciales
- /admin password: xaluca (ver /app/memory/test_credentials.md)
